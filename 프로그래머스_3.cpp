#include <string>
#include <vector>
#include <algorithm>
using namespace std;
int sum = 0;
long long solution(vector<int> a, vector<vector<int>> edges) {
    long long answer = -2;
    vector<vector<int>> dist[a.size() - 1];

    for (int i = 0; i < a.size() - 1; i++)
    {
        if (edges[i] != 0)
        {
            dist[edges[i].]
        }
    }

    sort(a[0], a[a.size() - 1]);
    for (int i = 0; i < a.size(); i++)
    {
        sum += a[i];
    }
    if (sum != 0)
    {
        return -1;
    }
    else
    {

    }



    return answer;
}

int main()
{
    solution({ -5,0,2,1,2 },)
}